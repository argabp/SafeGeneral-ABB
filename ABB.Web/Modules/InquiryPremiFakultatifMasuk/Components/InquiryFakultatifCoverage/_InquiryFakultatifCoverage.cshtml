@using ABB.Application.Akseptasis.Queries
@using Kendo.Mvc.UI

<script src="~/Modules/InquiryPremiFakultatifMasuk/Components/InquiryFakultatifCoverage/coverage.js"></script>

<div class="window-body">
    <div class="row">
        <div class="col-md-4">
            <input type="text" class="search-field-top" style="height: 1em" id="SearchKeywordCoverage" placeholder="Search"/>
        </div>
        <div class="offset-1"></div>
    </div>
    
    @(Html.Kendo().Grid<AkseptasiCoverageDto>()
        .Name("AkseptasiCoverageGrid")
        .DataSource(dataSource => dataSource.Ajax()
            .Read(read => read.Action("GetInquiryCoverages", "InquiryPremiFakultatifMasuk").Data("searchFilterCoverage"))
            .Model(model => model.Id(p => p.kd_cvrg))
        )
        .Columns(columns =>
        {
            columns.Command(command =>
            {
                command.Custom("ViewAkseptasiCoverage")
                    .Text("View")
                    .IconClass("fa fa-view")
                    .HtmlAttributes(new { title = "Edit" })
                    .Click("btnViewAkseptasiCoverage_OnClick");
            }).Width(200).Title("Action").Locked(true);
            columns.Bound(col => col.kd_cvrg).Width(150).Title("Kode Coverage");
            columns.Bound(col => col.kd_cvrg).Width(150).Title("Rate Premi").ClientTemplate("#= pst_rate_prm # #= stn_rate_prm == 1 ? '%' : '%o' #");
        })
        .Pageable(pager => pager
            .Refresh(true)
            .Info(true)
            .PageSizes(true)
        )
        .Sortable()
        .Filterable()
        .Scrollable()
        .AutoBind(false)
        .Height(450)
        .Resizable(a => a.Columns(true))
        .Events(ev => ev.DataBound("gridAutoFit"))
    
        )
    
    <div class="window-footer">
        <button type="button" id="btn-previous-akseptasiResikoCoverage" class="btn btn-primary space-right">
            <span class="fa fa-arrow-left"></span> Previous
        </button>
        <button type="button" id="btn-next-akseptasiResikoCoverage" class="btn btn-primary space-right">
            Next <span class="fa fa-arrow-right"></span>
        </button>
    </div>
</div>