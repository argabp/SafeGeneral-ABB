@using Kendo.Mvc.UI
@model ABB.Web.Modules.LimitAkseptasi.Models.LimitAkseptasiDetilViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, Kendo.Mvc
@addTagHelper *, ABB.Web

<div class="window-body" style="background-color: lightgray">
    <form id="LimitAkseptasiDetailForm" enctype="multipart/form-data" autocomplete="off">
        @Html.HiddenFor(p => p.kd_cb)
        @Html.HiddenFor(p => p.kd_cob)
        @Html.HiddenFor(p => p.kd_scob)
        @Html.HiddenFor(p => p.thn)
        <div class="form-group col-sm-12">
            <label class="col-sm-12">User</label>
            <div class="col-sm-12">
                <kendo-dropdownlist for="kd_user" style="width: 100%" class="primary-dropdown"
                                    datavaluefield="Value" datatextfield="Text" filter="FilterType.Contains">
                    <datasource type="DataSourceTagHelperType.Custom">
                        <transport>
                            <read url="@Url.Action("GetUsers", "LimitAkseptasi")"/>
                        </transport>
                    </datasource>
                </kendo-dropdownlist>
                <span asp-validation-for="kd_user" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group col-sm-12">
            <label class="col-sm-12">Limit (%)</label>
            <div class="col-sm-12">
                <kendo-numerictextbox for="pst_limit" decimals="2" max="100" style="width: 100%;"/>
                <span asp-validation-for="pst_limit" class="text-danger"></span>
            </div>
        </div>
    </form>
</div>

<div class="window-footer">
    <button type="button" onclick="onAddLimitAkseptasiDetail()" class="btn btn-success space-right">
        <span class="fa fa-save"></span> Save
    </button>
</div>